# Axiom — runtime dependencies.
#
# Deliberately small: every package here is actually imported by src/ or tests/. A
# requirements file that lists things the code never uses is a broken first impression —
# `pip install -r requirements.txt` is the first command anyone runs.
#
# The LLM layer calls Gemini and OpenAI over plain HTTPS (urllib, stdlib), so no vendor
# SDK is required; the policy retriever is TF-IDF over a small corpus, so no vector DB or
# embedding model is required either. Both were considered and dropped — see
# docs/project/DECISIONS.md.

# --- core ML / data ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
lightgbm>=4.3
shap>=0.45
joblib>=1.3
matplotlib>=3.8        # evaluation figures only

# --- graph (unsupervised fraud rings) ---
networkx>=3.2

# --- serving ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7

# --- optional: real Razorpay test-mode payment links (actuator degrades to a stub) ---
razorpay>=1.4

# --- tests ---
pytest>=8.0
