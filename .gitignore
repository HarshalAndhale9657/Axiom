# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Env / secrets
.env
*.key

# Data & model artifacts (keep small seed data; ignore large/generated)
data/*.csv
data/*.parquet
!data/seed_*.csv
models/*.pkl
models/*.txt
models/*.joblib

# RAG / audit stores
.chroma/
chroma_db/
*.sqlite
*.db

# Node / Next.js
web/node_modules/
web/.next/
web/out/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Personal scratch notes / raw secrets (kept locally, NEVER in the repo)
/Hackathon info.txt
/keys
/keys.txt

# Regenerable evaluation figures (final ones committed deliberately under docs/)
/reports/
